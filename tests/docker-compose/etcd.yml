version: '2'
services:
  etcd:
    image: elcolio/etcd
    volumes:
      - "/tmp/cowry/etcd:/data"
  etcd-borwser:
    image: buddho/etcd-browser
    ports:
      - "8000:8000"
    environment:
      - ETCD_HOST=etcd
      - ETCD_PORT=2379
